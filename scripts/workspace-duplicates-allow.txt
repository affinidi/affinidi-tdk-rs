# Workspace members knowingly shadowed by a crates.io copy.
#
# One crate name per line. Every entry is a debt marker, not a resolution:
# it means local edits to that crate are NOT what the workspace builds against
# in the affected subtree, and two copies of its types exist in the graph.
#
# Only add an entry when the duplicate cannot be fixed from this repository.
# A missing `[patch.crates-io]` entry is fixable here and does not belong.
#
#
# ---------------------------------------------------------------------------
#
# The affinidi-messaging-sdk and affinidi-tdk entries that lived here were
# cleared when the mediator moved to vta-sdk 0.38. They were pulled by a
# vta-sdk built against affinidi-messaging-sdk 0.19 / affinidi-tdk 0.8, which
# the workspace copies no longer satisfied, so `[patch.crates-io]` declined to
# apply. 0.38 is built against 0.24 and 0.14 — the versions this workspace
# holds — so both patches apply again and each crate resolves to one copy.
# This is the crossing the note below describes, completed.
#
#
# The affinidi-did-common entry that lived here was cleared on 2026-07-20:
# vta-sdk 0.19.14 shipped on didwebvh-rs 0.6, which requires
# affinidi-did-common "0.4", so the registry copy at 0.3.9 no longer enters the
# graph.
#
#
# ---------------------------------------------------------------------------
#
# The affinidi-messaging-sdk, affinidi-tdk and affinidi-messaging-mediator-common
# entries re-added 2026-09-15 for TSP Rev 3 were cleared on 2026-09-16, by the
# same crossing the note above describes — the fourth time, after 0.20, 0.21
# and 0.22.
#
# Rev 3 made affinidi-messaging-sdk 0.26 and affinidi-tdk 0.16. vta-sdk 0.40.0
# is built against those, so moving the mediator's requirement from "0.38" to
# "0.40" lets `[patch.crates-io]` apply again and drops the registry copies:
#
#     Removing affinidi-messaging-mediator-common v0.15.45
#     Removing affinidi-messaging-sdk v0.24.0
#     Removing affinidi-tdk v0.14.0
#     Removing trust-tasks-rs v0.20.5
#
# Worth recording what the duplicate actually cost, because it was more than
# two copies of a type: the frozen registry `affinidi-messaging-sdk` 0.24
# compiled against the *patched* `affinidi-messaging-core`, so any required
# field added to `Inbound` broke a published crate nobody could edit. The cycle
# was not untidy, it was **freezing this workspace's API against the registry**.
#
# The standing fix remains the one the mediator's Cargo.toml argues for —
# `affinidi-messaging-test-mediator` already takes it with
# `default-features = false`, so gating `vta-sdk` off there would end the cycle
# rather than re-time it. Note the escape is narrower than it reads: the
# mediator's own `mediator-setup` tool depends on `vta-sdk` unconditionally
# across 65 call sites, so gating the feature alone does not clear the
# duplicate. Tested 2026-09-16.



# ---------------------------------------------------------------------------
#
# Re-added 2026-09-23 for trust-tasks-rs 0.22 — the fifth crossing, and the
# same one the notes above describe, after 0.20, 0.21 and 0.22 (of this
# workspace's own lines) and Rev 3.
#
# 0.22 makes affinidi-messaging-sdk 0.27 and affinidi-tdk 0.17. The published
# vta-sdk 0.43 requires "0.26" and "0.16", which the local copies no longer
# satisfy, so `[patch.crates-io]` declines to apply and the registry copies
# enter the graph beside them.
#
# It clears the same way every previous crossing did, and the order is forced:
#
#   1. this PR publishes the messaging crates on 0.27 / 0.17
#   2. verifiable-trust-infrastructure takes trust-tasks-rs 0.22 — which it
#      cannot do until (1), because `MediatorAcl` crosses this SDK's API and a
#      graph holding two trust-tasks-rs fails to compile on it — and publishes
#      a vta-sdk built against 0.27 / 0.17
#   3. the mediator's vta-sdk requirement moves to that version, the patches
#      apply again, and both registry copies drop out
#
# Until (3) these two are duplicated, which is what the note above means by
# freezing this workspace's API against the registry: a required field added to
# a patched crate's type breaks the frozen registry copy of the SDK, which
# nobody can edit. Step (2) is unblocked the moment this merges and publishes.
affinidi-messaging-sdk
affinidi-tdk
