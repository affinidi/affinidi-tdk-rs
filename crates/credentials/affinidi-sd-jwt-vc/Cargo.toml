[package]
name = "affinidi-sd-jwt-vc"
description = "DEPRECATED — merged into affinidi-vc; use affinidi_vc::sd_jwt_vc. Thin re-export kept for one release."
version = "0.2.0"
edition.workspace = true
authors.workspace = true
readme = "README.md"
homepage.workspace = true
license.workspace = true
keywords = ["ssi", "affinidi", "sd-jwt-vc", "credentials"]
repository.workspace = true
publish.workspace = true
rust-version.workspace = true

# DEPRECATED: this crate re-exports affinidi_vc::sd_jwt_vc. All implementation
# (and its tests) moved to affinidi-vc; the only dependency is the new home.
[dependencies]
affinidi-vc = { version = "0.2", path = "../affinidi-vc" }
