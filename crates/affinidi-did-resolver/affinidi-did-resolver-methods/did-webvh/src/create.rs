/*!
*   Handles creating and adding to a webvh DID
*/

use crate::DIDWebVHError;

/// creates a new DID document and manages the log entry as required
fn create() -> Result<(), DIDWebVHError> {
    Ok(())
}
