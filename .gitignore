**/.DS_Store
**/.vscode
/target
environments.json
Cargo.lock
**/environments.json*

docs/*
# Architecture Decision Records are first-class, tracked docs.
!docs/adr/
# Security audit-readiness / threat-model docs are tracked.
!docs/security/
# Consumer migration guides are tracked.
!docs/migration/
# Testing guide + composed-stack docs are tracked.
!docs/testing/
# TSP usage cookbook is tracked.
!docs/tsp/

# mediator-setup wizard generated artifacts (may land in any directory)
**/conf/secrets.json
**/conf/secrets-admin.json
**/conf/keys/
# Exception: the docker-compose.test.yml stack's fixed TEST-ONLY identity is
# committed on purpose (see docs/testing/docker-compose.md). NEVER reuse it.
!docker/test/conf/secrets.json
**/bootstrap-request.json
**/bootstrap-request-vp.json
**/*.armor

# Superego
.superego/
CLAUDE.md
/docs/ssi-comparison-infographic.html

# Local agent state (Claude Code, etc.)
.claude/

# Secrets and credentials
.env
.env.*
